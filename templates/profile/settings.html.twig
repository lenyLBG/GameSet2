{% extends 'base.html.twig' %}

{% block title %}Paramètres du compte{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <h1>Paramètres du compte</h1>

            {% for label, messages in app.flashes %}
                {% for msg in messages %}
                    <div class="alert alert-{{ label }}">{{ msg }}</div>
                {% endfor %}
            {% endfor %}

            <form method="post" action="{{ path('app_profile_settings') }}" enctype="multipart/form-data">
                <input type="hidden" name="_token" value="{{ csrf_token('profile_settings') }}">

                <div class="mb-3">
                    <label for="prenom" class="form-label">Prénom</label>
                    <input type="text" class="form-control" id="prenom" name="prenom" value="{{ user.prenom|default('') }}">
                </div>

                <div class="mb-3">
                    <label for="nom" class="form-label">Nom</label>
                    <input type="text" class="form-control" id="nom" name="nom" value="{{ user.nom|default('') }}">
                </div>

                <div class="mb-3">
                    <label for="email" class="form-label">Email</label>
                    <input type="email" class="form-control" id="email" name="email" value="{{ user.email|default('') }}">
                </div>

                <div class="mb-3">
                    <label for="licence" class="form-label">Licence</label>
                    <input type="text" class="form-control" id="licence" name="licence" value="{{ user.licence|default('') }}">
                </div>

                <div class="mb-3">
                    <label for="avatar" class="form-label">Photo de profil</label>
                    {% if user.avatar is defined and user.avatar %}
                        <div class="mb-2">
                            <img src="{{ asset('uploads/avatars/' ~ user.avatar) }}" alt="avatar" class="rounded-circle" width="72" height="72">
                        </div>
                    {% endif %}
                    <input type="file" class="form-control" id="avatar" name="avatar" accept="image/*">
                    <div class="form-text">Formats acceptés : jpg, png. Taille max recommandée : 2MB.</div>
                </div>

                <div class="d-flex justify-content-between align-items-center">
                    <button class="btn btn-primary" type="submit">Enregistrer</button>
                    <a href="{{ path('app_profile') }}" class="btn btn-link">Retour au profil</a>
                </div>
            </form>

            <hr>

            <h3>Sécurité</h3>
            <p>Pour changer votre mot de passe, utilisez la fonctionnalité prévue par l'application.</p>

        </div>
    </div>
</div>
{% endblock %}
