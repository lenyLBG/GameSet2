{% extends 'base.html.twig' %}

{% block title %}Terrains{% endblock %}

{% block body %}
    <h1>Terrains</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ terrain.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ terrain.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_terrains_index') }}">back to list</a>

    <a href="{{ path('app_terrains_edit', {'id': terrain.id}) }}">edit</a>

    {{ include('terrains/_delete_form.html.twig') }}
{% endblock %}
